click==8.1.7
pypdf==4.1.0
pydantic==2.6.0
pyyaml==6.0.1
python-dotenv==1.0.0
rich==13.7.0
pillow==10.2.0
pngquant-cli==3.0.3
pymupdf>=1.24.0

# Optional: local LLM stack (L4). The pipeline runs fine without these — the
# `llm` and `feedback embed/search` subcommands need them.
ollama>=0.4.0
numpy>=2.0.0
